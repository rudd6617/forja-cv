# TODO — 2026-04-11

## Remaining

_(none — Phase 8 complete)_

## Done

### Phase 8: Local LLM Integration
- [x] Configure `LLM_API_URL` and `LLM_MODEL` in `.dev.vars` (oMLX: localhost:8888)
- [x] Mock mode now conditional on `!idToken` (no longer default when signed in)
- [x] oMLX backend support (commit a222719)

### Security
- [x] Add authentication to `/api/analyze` endpoint
- [x] Add single quote escaping to `escapeHtml`
- [x] Add delete confirmation dialog in ResumeList

### Error Handling
- [x] Replace silent `catch {}` with error display in ResumeList
- [x] Replace `alert()` with in-page error banner in Toolbar
- [x] Add 401 token expiry handling (auto-clear auth, show Sign In)
- [x] Add API timeout (15s CRUD, 130s analyze to match worker's 120s LLM timeout)
- [x] Consolidate `analyze.ts` error handling — reuse `handleResponse` from `resumeApi`
- [x] Add Authorization header to `/api/analyze` requests

### Accessibility
- [x] Add `aria-label` to layout and color buttons
- [x] Add `aria-label` to drag handle, expand toggle, remove button
- [x] Add `aria-expanded` to section headers and item toggles

### Code Quality
- [x] Remove dead `ToolbarState` type and `toolbar` field
- [x] Remove redundant `dragOverRef` (keep same-index guard)
- [x] Replace GSI `setTimeout` polling with `script.onload` Promise
- [x] `signOut` reuses `clearAuth` to eliminate duplication
- [x] Add `.catch()` to `gsiReady.then()` for script load failure
- [x] Mock mode text changed from hardcoded Chinese to English
- [x] `migrateData` no longer mutates input (shallow copy first)

### Print CSS
- [x] Add `@page margin`, `break-inside: avoid` for lists/h4/education

### Infrastructure
- [x] GitHub Actions CI workflow (lint + typecheck + test on push/PR)
- [x] Resume data schema versioning (`version` field + `migrateData` for legacy data)

### UX
- [x] Loading skeleton in ResumeList (animate-pulse cards)

### Testing
- [x] Set up Vitest + @testing-library/react + jsdom
- [x] `src/hooks/useResume.test.ts` — 16 tests (incl. migration)
- [x] `src/services/resumeApi.test.ts` — 11 tests
- [x] `worker/index.test.ts` — 23 tests
